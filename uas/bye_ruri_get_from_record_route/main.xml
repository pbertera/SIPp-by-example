<?xml version="1.0" encoding="UTF-8" ?>
<scenario>
    <!-- By adding rrs="true" (Record Route Sets), the route sets         -->
    <!-- are saved and used for following messages sent. Useful to test   -->
    <!-- against stateful SIP proxies/B2BUAs.                             -->
    <recv request="INVITE" crlf="true" rrs="true">
        <action>
            <!-- since we need to send a request to the remote part -->
            <!-- we need to extract the Contact and the From/To header content -->
            <ereg regexp=".*" search_in="hdr" header="From" assign_to="remote_from"/>
            <ereg regexp=".*" search_in="hdr" header="To" assign_to="remote_to"/>

            <!-- assign the content of the Contaact SIP URI to the remote_contact var -->
            <!-- first var of assign_to contains the whole match -->
            <!-- <ereg regexp="sip:(.*)>.*" search_in="hdr" header="Contact" assign_to="trash_contact,remote_contact"/> -->
            <!-- <ereg regexp=".*(sip:.*)>" search_in="hdr" header="Record-Route" assign_to="trash_record_route,remote_record_route"/> -->

            <!-- <ereg regexp=".*(sip:[0-9]{3}.[0-9]{3}.[0-9]{3}.[0-9]{3}:[0-9]{5});.*>" search_in="hdr" header="Record-Route" assign_to="trash_record_route_sip,remote_record_route_sip"/> -->
            <ereg regexp=".*(;alias=.*);.*>" search_in="hdr" header="Record-Route" assign_to="trash_record_route_alias,remote_record_route_alias"/>
        </action>
    </recv>

    <!-- <Reference variables="trash_contact"/> -->
    <!-- <Reference variables="trash_record_route"/> -->
    <Reference variables="trash_record_route_alias"/>
    <!-- <Reference variables="trash_record_route_alias"/> -->


    <!-- The '[last_*]' keyword is replaced automatically by the          -->
    <!-- specified header if it was present in the last message received  -->
    <!-- (except if it was a retransmission). If the header was not       -->
    <!-- present or if no message has been received, the '[last_*]'       -->
    <!-- keyword is discarded, and all bytes until the end of the line    -->
    <!-- are also discarded.                                              -->
    <!--                                                                  -->
    <!-- If the specified header was present several times in the         -->
    <!-- message, all occurences are concatenated (CRLF seperated)        -->
    <!-- to be used in place of the '[last_*]' keyword.                   -->

    <send>
        <![CDATA[

        SIP/2.0 180 Ringing
        [last_Via:]
        [last_From:]
        [last_To:];tag=[call_number]
        [last_Call-ID:]
        [last_CSeq:]
        Contact: <sip:[local_ip]:[local_port];transport=[transport]>
        Content-Length: 0

        ]]>
    </send>

    <send retrans="500">
        <![CDATA[

        SIP/2.0 200 OK
        [last_Via:]
        [last_From:]
        [last_To:];tag=[call_number]
        [last_Call-ID:]
        [last_CSeq:]
        Contact: <sip:[local_ip]:[local_port];transport=[transport]>
        Content-Type: application/sdp
        Content-Length: [len]

        v=0
        o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
        s=-
        c=IN IP[media_ip_type] [media_ip]
        t=0 0
        m=audio [media_port] RTP/AVP 0
        a=rtpmap:0 PCMU/8000

        ]]>
    </send>

    <recv request="ACK"
        rtd="true"
        crlf="true">
    </recv>

    <pause milliseconds="3000"/>

    <send retrans="500">
        <![CDATA[
        BYE 35.196.62.91:5060 SIP/2.0
        Via: SIP/2.0/[transport] [local_ip]:[local_port]
        [routes]
        To[$remote_from]
        From[$remote_to];tag=[call_number]
        [last_Call-ID:]
        CSeq: [cseq] BYE
        Max-Forwards: 32
        Contact: <sip:[local_ip]:[local_port];transport=[transport]>
        Content-Length: 0
        ]]>
    </send>

    <recv response="200">
    </recv>


    <!-- Keep the call open for a while in case the 200 is lost to be     -->
    <!-- able to retransmit it if we receive the BYE again.               -->
    <pause milliseconds="4000"/>


    <!-- definition of the response time repartition table (unit is ms)   -->
    <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>

    <!-- definition of the call length repartition table (unit is ms)     -->
    <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000"/>

</scenario>

